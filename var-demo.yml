---
  - name: This is a demo variable file
    hosts: localhost
    become: yes
    vars: 
      user: demo
    tasks:
      - name: create a user
        user:
          name: "{{user}}"
          state: present
...