---
  - name: This is a demo variable file
    hosts: localhost
    vars: 
      user: demo
    tasks:
      - name: create a user
        user:
          name: "{{user}}"
          state: present
...