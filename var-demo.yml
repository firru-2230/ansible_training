---
  - name: This is a demo variable file
    hosts: all
    become: yes
    vars: 
      user: demo
    tasks:
      - name: create a user
        user:
          name: "{{user}}"
          state: present
...